/* 
 * File:   AddModules.h
 * Author: Sebastian Junges
 *
 * Created on January 12, 2013, 3:39 PM
 */

#pragma once 

#include "../Manager.h"
#include "ModuleType.h"
#include "Modules.h"
#include "../config.h"
#include "../RuntimeSettings.h"

#include <list>

namespace smtrat {
std::list<RuntimeSettings*> addModules(Manager* manager) {
		// List of settings objects
		std::list<RuntimeSettings*> settingsObjects;
		//
        // Add all existing modules, as well as the settings
        //
        @addModuleList@
		// Return he list of settings objects, so any method managing them gets them.
		return settingsObjects;
}
}


