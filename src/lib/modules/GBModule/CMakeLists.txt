set(SMTRAT_GROEBNER_Settings "5" CACHE STRING "Number of setting used for GroebnerModule")
BeginDefineModule()
ModuleMainHeader(GBModule/GroebnerModule.h)
FILE(GLOB_RECURSE sources *.cpp)
foreach(src ${sources})
    AddModuleSource(${src})
endforeach()
ModuleName(GroebnerModule)
ModuleClass(GroebnerModule<GBSettings${SMTRAT_GROEBNER_Settings}>)
ModuleVersion(0 5 0)
ModuleSettingsClass(GBRuntimeSettings)
ModuleSettingsName(output)
EndDefineModule(gbEnabled)

if(${gbEnabled})
	# do something
endif()