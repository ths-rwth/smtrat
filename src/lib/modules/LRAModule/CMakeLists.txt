BeginDefineModule()
ModuleMainHeader(LRAModule/LRAModule.h)
FILE(GLOB_RECURSE sources *.cpp)
foreach(src ${sources})
    AddModuleSource(${src})
endforeach()
ModuleName(LRAModule)
ModuleClass(LRAModule)
ModuleVersion(0 0 0)
ModuleDefaultEnabled()
EndDefineModule(enabled)

if(${enabled})
	# do something
endif()