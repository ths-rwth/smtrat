BeginDefineModule()
ModuleMainHeader(TLRAModule/TLRAModule.h)
FILE(GLOB_RECURSE sources *.cpp)
foreach(src ${sources})
    AddModuleSource(${src})
endforeach()
ModuleName(TLRAModule)
ModuleClass(TLRAModule)
ModuleVersion(0 0 0)
ModuleDefaultEnabled()
EndDefineModule(enabled)

if(${enabled})
	# do something
endif()