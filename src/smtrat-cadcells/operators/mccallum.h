#pragma once

#include "projection.h"

namespace smtrat::cadcells::operators::mccallum {

cell_at_level construct_cell(properties& properties, const var_order& order, const Model& sample) {
    
}
    
}
